# 작업 기록 (WORKLOG)

작업할 때마다 맨 위에 날짜 블록을 새로 추가한다.
사이트 구조·기술 원칙은 [README.md](./README.md), 이미지 규칙은 [IMAGE_GUIDE.md](./IMAGE_GUIDE.md) 참조.

---

## 현재 상태 — 새 세션이면 여기부터

**최신 커밋: `c7ec5b7` (2026-08-26)** · 브랜치 `main` · 작업 폴더 `C:\Users\User\Desktop\바로철거`

사이트 자체는 **사실상 완성 상태**다. 6개 페이지(메인·철거예약·폐기물예약·시공사례·고객센터·개인정보처리방침),
이미지 16곳, 사업자 정보까지 전부 채워져 있고 화면에 노출되는 대괄호 플레이스홀더는 하나도 없다.

**남은 건 코드가 아니라 외부 자료·계정 문제다.**

| 막고 있는 것 | 상태 |
|---|---|
| 호스팅 | 카페24가 쇼핑몰 호스팅(EC)이라 정적 HTML을 올릴 자리가 없음 → 닷홈 발급 대기 |
| 폼 접수 주소 | Google Apps Script 미배포 → 온라인 접수 불가 (화면은 안 깨짐, 전화 안내로 대체 중) |
| 검색엔진 인증 | 네이버·구글 인증코드 미수령 |

원격(`origin/main`)은 `544584f`에 멈춰 있다. 그 뒤 커밋 6건은 **로컬에만 있다.**

---

## 2026-08-26 (수)

커밋 4건 — `55f72bc` → `f1caf42` → `eed0ced` → `c7ec5b7`

### 1. 시공 사진 교체본 반영 `55f72bc`

세션 사이에 교체되어 있던 사진 10개(히어로 1·시공사례 6·갤러리 3)를 별도 커밋으로 분리해 담았다.
처음엔 아래 코드 수정과 한 커밋에 섞였는데, 성격이 달라 커밋을 나눠 다시 만들었다. → [주의사항 6번](#6-커밋-전-git-status를-먼저-본다)

### 2. 받은 피드백 4건 반영 `f1caf42`

| 지적 | 조치 |
|---|---|
| 갤러리 안내는 "사례 번호를 말씀해 달라"인데 사진에 번호가 없음 | 문구를 바꾸는 대신 **번호를 실제로 붙임.** 갤러리 9칸 + 메인 시공사례 6칸에 배지(`.case-no`) 추가. 메인 01~06과 갤러리 01~06은 파일명 기준으로 같은 번호. 라이트박스 캡션도 "사례 03 · …" |
| 모바일 예약 페이지에서 4단계 절차를 다 지나야 폼이 나옴 | DOM 순서를 **폼 → 절차**로 뒤집음. 375px 기준 폼 시작 위치 1344px → **356px**. 데스크톱은 grid 배치로 좌:절차 / 우:폼 그대로 |
| 장비 섹션 25대 직후 실적 섹션에서 같은 숫자 반복 | 실적 2번 지표를 "적법 / 폐기물 처리 전 과정"으로 교체, 3번 부연에 행정 서류 대응 명시, 제목 "10년이 남긴 것"으로 조정 |
| 모바일 메뉴·라이트박스가 열려도 배경이 키보드 탐색에 남음 + 메뉴 위에 하단 상담 바가 보임 | 공통 `makeOverlay()` 신설 — 배경 `inert` + `aria-hidden`, Tab 가두기. 메뉴는 첫 링크로 포커스 이동 · Esc로 닫고 햄버거 복귀 · 배경 스크롤 잠금 · 하단 바 숨김. 라이트박스는 닫을 때 보던 사진의 카드로 포커스 반환 |

작업 중 발견해서 같이 고친 것 —

- **투명한 전체화면 오버레이 위험** — `.gnb` 트랜지션에 `visibility`가 묶여 있었다. 트랜지션이 진행되지 않는 상황에서는 메뉴가 *안 보이는데 화면 전체를 덮은 상태*로 남아 뒤쪽 클릭이 전부 막힌다. 트랜지션에서 `visibility`를 빼서 상태가 어긋날 수 없게 함
- **라이트박스 닫을 때 포커스 유실** — 닫으면 포커스가 body로 흩어져 키보드 사용자가 위치를 잃었다. 보던 사진의 카드로 반환하도록 수정

검증 — 360 / 375 / 1280 / 1440px 가로 스크롤 없음 · 콘솔 오류 0건 ·
메인 8개 섹션 1280×720에서 100vh 유지(초과 0건) · Tab 가두기와 Esc는 실제 키 입력으로 확인

### 3. 사업자 정보 확정 `eed0ced`

- 대표: **김은정 · 차슬기** (공동대표) — 6개 페이지 푸터
- 이메일: **ssimyong@naver.com** — 푸터·개인정보처리방침에 mailto 링크로 연결,
  고객센터 발주서 안내에도 보낼 주소를 직접 표기, JSON-LD에 `email` 추가
- 개인정보 보호책임자는 한 명을 지정하는 자리라 **김은정** 대표로 기입 (변경 필요 시 확인받을 것)
- 푸터 메일 주소에 옅은 밑줄 + 호버 앰버를 넣어 링크임을 드러냄
- 이로써 화면에 노출되던 `[대표자명]`·`[이메일]` 대괄호가 전부 사라짐

### 4. 호스팅 방향 확정 (코드 변경 없음)

고객 카페24 서비스가 **쇼핑몰 호스팅(EC)** 으로 확인됐다. 웹호스팅이 아니라 쇼핑몰 솔루션이라
용량 항목 자체가 표시되지 않고, 도메인 루트를 쇼핑몰 엔진이 점유해 **정적 HTML을 올릴 자리가 없다.**

결론 — 닷홈에서 호스팅만 발급받고 **도메인은 카페24에 그대로 두고 네임서버만 변경**한다.
기관이전은 비용·대기시간·60일 제한이 붙는데 얻는 게 없어서 하지 않는다.

### 5. 환경 정리 (저장소와 무관)

Claude 앱 캐시 10.1GB 중 VM 이미지 `claudevm.bundle`을 지워 **7.75GB 회수**
(패키지 폴더 10,151MB → 1,614MB). 실행 파일·세션 기록·로그인 정보는 건드리지 않았다.

`sessiondata.vhdx`(580MB)만 확신이 없어 `C:\Users\User\claude-vm-backup\`으로 빼뒀다.
**한동안 앱을 써서 문제없으면 이 백업은 지워도 된다.**

---

## 2026-08-25 (화)

커밋 3건 — `ae41061` → `15cb7d6` → `8b12c78`

### 1. 배포 전 정리 `ae41061`

| 항목 | 내용 |
|---|---|
| 가짜 공지 제거 | `support.html` 공지사항에 날짜가 `2026.08.00`인 시안용 공지 3건이 `href="#"` 죽은 링크로 걸려 있었음. 실제인 오픈 공지 1건만 남기고 삭제 |
| 공지 마크업 변경 | 공지 상세 페이지가 없는 구조 → 링크 대신 `.n-item` 사용. `pages.css`의 `.notice-list a` 셀렉터에 `.n-item` 병기 |
| 예약 폼 실패 문구 | `FORM_ENDPOINT`가 `[APPS_SCRIPT_URL]` 그대로여서 제출 시 그 문자열로 POST → 404 → "전송에 실패했습니다"가 뜨던 문제. 주소가 비면 전송을 시도하지 않고 "온라인 접수 준비 중입니다 + 전화 상담"을 띄우게 수정 |
| 문서 갱신 | README의 배포 전 항목표를 실제 상태 + *미설정 시 동작*까지 적도록 갱신, 이미지 교체 완료로 무의미해진 주석 3곳 제거 |

### 2. 폐업지원금 섹션 스타일 `15cb7d6`

**증상** — 바탕화면 폴더에서 폐업지원금 구간만 스타일 없는 텍스트 더미로 보임.
**원인** — `index.html`에 섹션 마크업만 추가되고 `pages.css`에 `.closing` 계열 스타일이 **한 줄도 없었음.** (CSS 자체는 정상 로드 — 새 섹션 몫만 없던 것)

- `.closing` 신규 작성 — 배경 `--color-ink`, 금액 앰버, 헤어라인 구분선 (기존 섹션과 같은 문법)
- 좌측 카피 / 우측 지원금액 2단, 진행 4단계는 데스크톱 4열 → 태블릿 2×2 → 모바일 1열
- 스냅 모드 압축값 추가 — 720px 높이에서도 100vh 안에 수렴
- `최대`를 `.cf-pre`로 분리해 금액 숫자와 크기 구분 (기존 `stat`/`suffix` 패턴과 동일)
- 같은 커밋에 이전 세션의 미커밋분(파비콘 `barofav.png`, `KAKAO_URL` 오픈채팅 주소) 포함

### 3. 문단 줄바꿈 정리 `8b12c78`

"…처리해 **드립니다.**" 처럼 마지막 줄에 단어가 하나만 떨어지던 문단 정리.

- **`.ln`** (신설) — 의미 단위 블록. 주요 리드 문단은 끊길 자리를 직접 지정
- **`text-wrap: balance`** — 나머지 본문 일괄 (강점·시공범위·FAQ·안내문 등)
- 6개 페이지 × 6개 폭 전수 측정 → 마지막 줄이 짧게 남는 문단 0건

---

## 다음에 할 일

### 🔴 배포를 막고 있는 것

| 항목 | 위치 | 상태 |
|---|---|---|
| 호스팅 | — | 닷홈 가입·호스팅 발급 → 네임서버 변경. **고객이 직접 가입해야 함** (소유권이 고객에게 있어야 안전) |
| 폼 접수 주소 | `assets/js/form.js` 상단 `FORM_ENDPOINT` | Google Apps Script 배포 후 URL 기입. 접수 알림은 ssimyong@naver.com으로 받게 설정 |
| 네이버·구글 인증코드 | 각 페이지 `<head>` 주석 | 검색엔진 등록 시 필요 |
| FTP 업로드 | — | 위 호스팅이 잡히면 파일질라로 업로드. 업로드 대상은 약 7MB (발주서 PDF 4.5MB 포함) |

### 🟡 확인받아야 진행

- **"홈페이지 주소"** — 이메일을 주시며 "홈페이지 주소는 이거야"라고 하셨는데, 이메일을 가리킨 건지 도메인을 따로 주시려던 건지 미확인. 현재 사이트는 **direct-demolition.com** 기준으로 canonical·OG·sitemap이 모두 설정돼 있음. 도메인이 바뀌면 그 값들도 함께 수정해야 함
- **개인정보 보호책임자** — 김은정 대표로 기입해둠. 차슬기 대표나 다른 분으로 지정할지 확인
- **안전 관리 지표** — 실적 섹션에 넣자는 의견이 있었으나 무재해 일수·보험 가입 같은 수치는 지어낼 수 없어 보류. 실제 자료를 받으면 지표 한 칸 교체 또는 4칸으로 확장
- **공지사항** — 실제 공지 추가 여부 (현재 오픈 공지 1건)

### 🟢 여유 될 때

- `assets/img/og.png` 1.17MB → 공유 미리보기용치곤 큼 (권장 300KB 이하). JPG 재출력 검토
- `assets/img/icon/tel200.png` 미사용 (iOS 기본 전화 아이콘이라 인라인 SVG로 대체) — 삭제 여부 결정
- `C:\Users\User\claude-vm-backup\sessiondata.vhdx` (580MB) — 문제없으면 삭제
- 원격 푸시 — 로컬 커밋 6건이 `origin/main`에 안 올라가 있음

---

## 작업 시 반드시 기억할 것

### 1. worktree ≠ 바탕화면 폴더

AI 세션이 `.claude/worktrees/...`(별도 폴더·별도 브랜치)에서 열리면, 거기서 커밋한 내용은
**`C:\Users\User\Desktop\바로철거`에 나타나지 않는다.**
작업 후 반드시 `main`으로 병합할 것. 브라우저에서 안 보이면 이것부터 의심한다.

```bash
git -C "C:/Users/User/Desktop/바로철거" merge --ff-only <브랜치명>
```

### 2. 새 섹션을 추가하면 CSS도 같이

마크업만 추가하고 스타일을 빼먹으면 그 구간만 텍스트 더미로 쏟아진다 (`15cb7d6` 사례).
메인은 `body[data-snap]` 풀페이지라 **새 섹션도 자동으로 스냅 순서에 편입**되므로,
`pages.css` 맨 아래 스냅 블록에 세로 압축값(vh 단위)을 같이 넣어 100vh를 넘기지 않게 한다.

### 3. 100vh 예산

문단에 줄이 하나 늘면 섹션이 100vh를 넘길 수 있다.
실제로 장비 섹션 설명을 두 줄로 고정했다가 11px 초과해서 되돌렸다.
텍스트를 손대면 **1280×720에서 메인 8개 섹션 높이를 다시 확인**할 것.

### 4. 한국어 줄바꿈 규칙

- 새 리드 문단을 쓸 땐 의미 단위로 `<span class="ln">`으로 감싼다
- 짧은 본문은 감싸지 말고 `text-wrap: balance`에 맡긴다 (이미 `common.css`에 지정됨)
- 한 `<p>` 안에서 `<br>`만 쓰면 뒷문장이 균형을 잃어 또 단어 하나가 남는다 — `.ln`을 쓸 것

### 5. 미설정 값은 화면이 깨지지 않게

카카오 URL·폼 주소처럼 나중에 받는 값은 **비어 있을 때의 동작**을 반드시 만든다.
(카카오 버튼 자동 숨김 / 폼은 전화 상담 안내로 대체)

### 6. 커밋 전 `git status`를 먼저 본다

세션 사이에 이미지가 교체되는 등 **내가 만들지 않은 변경이 작업 폴더에 남아 있을 수 있다.**
그대로 `git add -A` 하면 남의 변경이 내 커밋에 섞인다 (2026-08-26에 사진 10개가 그렇게 섞여 커밋을 나눠 다시 만들었다).
커밋 전에 목록을 훑고, 성격이 다른 변경은 커밋을 분리한다.

### 7. 사실이 아닌 수치는 쓰지 않는다

무재해 일수·보험 가입·시공 건수처럼 **확인되지 않은 숫자는 채우지 않는다.**
관공서 발주를 받는 업체라 표기가 사실과 다르면 문제가 커진다.
빈칸으로 두고 자료를 요청하거나, 이미 사이트가 하고 있는 주장(직영 구조·적법 처리 등) 안에서 고른다.

### 8. 브라우저 검증의 함정

미리보기 패널이 화면에 표시되지 않으면 **CSS 트랜지션과 `requestAnimationFrame`이 아예 진행되지 않는다.**
그 상태에서 측정하면 "opacity가 0에서 안 변한다", "포커스가 안 옮겨진다" 같은 가짜 증상이 나온다.
스크린샷이 `not compositing frames` 오류로 실패하면 이 상태이니, 타이밍에 의존하는 결론을 내리지 말 것.

### 9. 미리보기 서버

- `.claude/serve.js` — 열려 있는 폴더 (포트 8340)
- `.claude/serve-main.js` — 바탕화면 배포 폴더 (포트 8341) · 실제로 보이는 화면을 확인할 때 사용
- `.claude/` 와 `.playwright-cli/` 는 `.gitignore` 처리됨 — 커밋에 딸려 들어가지 않게 주의
